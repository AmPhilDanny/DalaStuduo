# Mediasoup: use pre-built worker binary, skip C++ compilation.
# Without this, mediasoup compiles its worker from C++ source which
# requires 6-8GB RAM and will OOM on Render's build machines.
mediasoup_worker_bin=
